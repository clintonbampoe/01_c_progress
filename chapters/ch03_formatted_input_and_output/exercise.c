// This program is for testing the random variables

#include <stdio.h>

int main(void) {
    int one, two, three;
    float four, five, six;

    printf("%d, %d, %d,", one, two, three);

    printf("%f, %f, %f, ", four, five, six);
    return 0;
}